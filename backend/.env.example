# ==========================================
# CMES-ADMIN Backend — Environment Variables
# ==========================================
# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง
# cp .env.example .env

# --- Server ---
PORT=5001
NODE_ENV=development

# --- MongoDB Atlas ---
# สร้าง cluster ฟรีที่ https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/cmes-admin
# Optional, only for the local queue load-test if Node cannot resolve Atlas SRV records.
# Example: MONGODB_DNS_SERVER=192.168.1.1
# MONGODB_DNS_SERVER=

# --- JWT Authentication ---
# สร้าง secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ADMIN_JWT_SECRET=change-this-to-a-64-char-random-hex-string
# Must exactly match CMES-USER backend USER_SERVICE_TOKEN. Never expose it to a browser.
USER_SERVICE_TOKEN=change-this-to-a-separate-64-char-random-hex-string

# --- Frontend URLs (CORS whitelist) ---
# LOCAL:
USER_FRONTEND_URL=http://localhost:3001
ADMIN_FRONTEND_URL=http://localhost:3000
# PRODUCTION:
# USER_FRONTEND_URL=https://your-user-frontend.vercel.app
# ADMIN_FRONTEND_URL=https://your-admin-frontend.vercel.app

# --- Cloudinary (File Storage) ---
# สร้างบัญชีฟรีที่ https://cloudinary.com
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# --- SightEngine AI Content Moderation ---
# สมัครฟรีที่ https://sightengine.com (Free: 2,000 images/month)
SIGHTENGINE_API_USER=your-api-user
SIGHTENGINE_API_SECRET=your-api-secret

# --- Application Settings ---
EXPECTED_AMOUNT=100
# Active queue cap per logged-in user / sender phone. Completed and rejected
# items do not count. The default is 3 if this variable is omitted.
MAX_ACTIVE_QUEUE_PER_USER=3
